name: Test Echo
on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to echo'
        required: true
        type: string
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.message }}"
